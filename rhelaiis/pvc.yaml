apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-cache
  namespace: rhel-lightspeed-ai
  labels:
    app: rhelaiis
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi